\# 🛠 TECHNICAL SPECIFICATIONS: Master Dashboard \& Solvency UI



\## 1. Цель модуля

\- Разработка интерфейса панели управления для Мастера (экран S-09).

\- Внедрение визуальной системы "Solvency Check" (проверка платежеспособности).

\- Блокировка основного функционала (сканера QR), если депозит Мастера ниже допустимого порога.



\## 2. Функциональные требования

\- \[ ] Создать экран `MasterDashboardScreen` (S-09).

\- \[ ] Интегрировать слушатель (Stream/Provider) параметра `depositBalance` и `isVisible` из модели пользователя.

\- \[ ] \*\*Логика блокировки:\*\* Если `isVisible == false`, кнопка перехода к сканеру (S-10) должна быть неактивна (Disabled).

\- \[ ] \*\*Визуальный Алерт:\*\* Если `isVisible == false`, в верхней части экрана должен отображаться мигающий красный баннер "Low Balance! Scanner locked. Top up required." с кнопкой "Top Up" (переход на S-17).



\## 3. UI/UX и Визуализация (Dubai Luxury Style)

\- \*\*Цвета:\*\* Использовать `AppTheme.deepBlack` для фона, `AppTheme.primaryGold` для активных элементов.

\- \*\*Алерт:\*\* Использовать цвет `#CF6679` (Soft Red) для баннера блокировки. 

\- \*\*Элементы:\*\* Карточка текущего депозита (Deposit Balance Card) крупным шрифтом (Inter/System). Ниже — статистика (сколько лидов, сколько сделок в этом месяце).



\## 4. Обработка ошибок (Edge Cases)

\- \*\*Мгновенная разблокировка:\*\* При пополнении баланса (когда Cloud Function переключит `isVisible` обратно на `true`), UI должен обновиться реактивно без перезагрузки приложения (используем GetX / Riverpod).

